import {observer} from "mobx-react"
import React from "react"
import {View} from "react-native"

function LeaderboardScreenComponent() {
  return <View></View>
}

export const LeaderboardScreen = observer(LeaderboardScreenComponent)
